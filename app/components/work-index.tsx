import { ReviewIndex } from "./review-index";

export function WorkIndex() {
  /* Explore remains in the codebase while it is being rebuilt, but it is not
     a public reading mode. Every homepage URL renders the review. */
  return <ReviewIndex />;
}
